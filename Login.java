package classes;

public class Login{

	//instance fields
	User user;
	
    // constructor
    public Login(User user) {
        this.user = user;	
    }
    
    // set methods
    public void loginReviewer(user) {
    	if (isreviewer)
    	    return true;
    	else
    		return false;
    }
    
    public void loginAuthor(user) {
    	if (isauthor)
    	    return true;
    	else
    		return false;
    }
    
    public void logineditor(user) {
    	if (iseditor)
    	    return true;
    	else
    		return false;
    }
    
}
